<%= form_for(@enrollments_score) do |f| %>
  <% if @enrollments_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@enrollments_score.errors.count, "error") %> prohibited this enrollments_score from being saved:</h2>

      <ul>
      <% @enrollments_score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_enrollment %><br>
    <%= f.number_field :id_enrollment %>
  </div>
  <div class="field">
    <%= f.label :id_score %><br>
    <%= f.number_field :id_score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
